#include "blinn_phong_material.h"

// Program* BlinnPhongMaterial::program = nullptr; // Uhhh, how do I make it static if it's not static in the base calss Material???

// BlinnPhongMaterial::BlinnPhongMaterial(std::string vs_path, std::string fs_path) {
//     VertexShader* vs = new VertexShader(vs_path);
//     FragmentShader* fs = new FragmentShader(fs_path);

//     program = new Program(vs, fs);
// }